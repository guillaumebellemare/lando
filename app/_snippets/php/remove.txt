/*
|--------------------------------------------------------------------------
| Remove Snippet
|--------------------------------------------------------------------------
|
| Arguments : nom de la table, id de la rangée à supprimer
|
| @return $messages, $errors
|
*/

function remove() {
	$this->remove(Base::table(), $_GET['argb']);
}
